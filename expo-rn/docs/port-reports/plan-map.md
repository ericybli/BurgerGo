Gate result: 2 errors total, both in `screens/plan/PlanScreen.tsx` (sibling-owned, mid-update to the shared contract). Zero errors reference my assigned paths.

FILES CREATED/CHANGED
- /Users/eric/own/BurgerGo/expo-rn/screens/plan/PlanMap.native.tsx — rewritten: react-native-maps canvas (PROVIDER_DEFAULT), Atlas pins via Marker anchor {0.5,0.5} + tracksViewChanges flip, per-leg visible+invisible(16px tappable) polylines, walk lineDashPattern [1,10] round cap, onPoiClick gated by toggle, fit-once-per-position-key via fitToCoordinates, fullscreen = style swap on a single MapView (no remount) + Android BackHandler, locate via expo-location (graceful denial, blue dot marker, pan + raise-zoom-only).
- /Users/eric/own/BurgerGo/expo-rn/screens/plan/PlanMap.web.tsx — rewritten: module-level Google Maps JS loader (literal `process.env.EXPO_PUBLIC_GOOGLE_MAPS_BROWSER_KEY` read; EmptyState if missing/failed), map mounted into a div appended inside an RNW View callback ref, disableDefaultUI, DOM pins via OverlayView + markerDom, walk dots via circle symbols (strokeOpacity 0, repeat 10px), 16px strokeOpacity-0.001 hit lines, POI via map click placeId + e.stop() + clickableIcons bound to toggle, fitBounds keyed on position key, size-guarded ResizeObserver (card opens never refit), fullscreen = position:fixed style swap + Escape, locate via navigator.geolocation.
- NEW /Users/eric/own/BurgerGo/expo-rn/screens/plan/map/: mapData.ts (pin/leg-seg/route-link/fit-key builders; leg lookup keyed `from|to|mode` with `legMode ?? dayModes[date] ?? 'drive'`; straight-line fallback; SAVED #33677A / RESTAURANT #C99231), useMapShell.ts (all shared state: layers, fullscreen, satellite, leg chip + auto-clear, POI card, restaurant card, AsyncStorage-persisted "bg.poiEnabled", derived pins/segs/legend/routeLinks/offline list, pin-tap routing), Pin.tsx (RN pin: white disc + 2px ring + glyph 34/28px, orderIndex+1 badge, white hairline time pill, symmetric wrapper so disc center = coordinate), markerDom.ts (DOM port of web markerEl.ts), MapChrome.tsx (Layers pill+menu, Maximize/Minimize, Satellite/Map pill, LocateFixed, Landmark POI toggle accent-ON), LegChip.tsx ("{from} → {to}" + formatLeg miles or "—", ✕), DayLegend.tsx ("All days" + Day N chips, ink-active/white dot, onSelectDate), RouteLinks.tsx (single direct / collapsible "Open day routes…", day-color dots, Linking.openURL, dayRouteUrl with place ids), PoiCard.tsx (full F-section port: photo pager w/ ‹ › + n/m pill via api.google.poiPhotoUrl(ref,800), gold #C99231 star + toFixed(1) + review(s) count, address, summary, Open now/Closed + expandable hours, reviews clamped 4 lines, exact labels "Adding…"/"Saved ✓"/"Added to day ✓"/"Saved to Eats ✓", orange Save restaurant vs orange Add to day + outline Save place, inline day-picker sheet from dayGroups, accent "Open in Google Maps", disabled offline, keyed by placeId, fallback name "Map landmark"), RestaurantCard.tsx, OfflineMap.tsx ("Map needs a connection" + placeUrl deep-link rows).

SPEC ITEMS COMPLETED: gaps 1–9, 11–13 fully; legend shows even for 1 day; "Nothing to map here" kept for online-but-empty (with legend still visible for recovery); both platform files `export type { MapDayGroup, MapRestaurant } from './PlanMap.types'` and default-export against the exact PlanMapProps contract.

NOT DONE / LIMITED (with reasons)
- Gap 10 (richer restaurant card): the read-only contract's MapRestaurant only carries id/name/lat/lng/cuisine, so the restaurant pin card omits address/notes/photo and its placeUrl uses name+coords only. Needs a contract change to fully match web section J.
- Native POI toggle hidden on iOS Expo Go: onPoiClick is Google-provider-only; PROVIDER_DEFAULT = Apple Maps on iOS (per spec edge note). Works on Android native and on web.
- Native fullscreen expands over the PlanMap component bounds (legend/route links hidden) but cannot cover PlanScreen's day strip/tab bar — RN absolute positioning can't escape the parent without a remounting Modal, and the spec forbade remount; viewport is preserved exactly.
- POI save side-effects (google.details photo-cache, day recompute, AI summary) are the PlanScreen owner's responsibility per the contract (onPoiSavePlace/onPoiAddToDay/onPoiSaveRestaurant promises); the card only drives busy/added states off those promises.

CONTRACT ASSUMPTIONS
- selectedDate=null ⇒ all days visible; legend "All days" → onSelectDate(null), day chip → onSelectDate(date); legend entry active when its day is visible (all active under "All days", matching web aria-pressed semantics).
- MapDayGroup.color is the authoritative day color (PlanScreen computes it via theme dayColor); restaurants pin uses #C99231 per web RESTAURANT_PIN_COLOR (not the #C9842E in the task summary).
- POI/restaurant/leg overlays render over the map box (whole screen when fullscreen) rather than the web's full-viewport fixed modal.
- The two current tsc errors (PlanScreen.tsx lines 211, 254) are pre-existing/transient in the sibling's file: it still passes the seed prop set and a stale PlaceViewSheet prop; they resolve when PlanScreen adopts the contract.