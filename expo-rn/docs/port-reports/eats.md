Gate passes with zero errors. Final report:

## Eats section port — report

**Files changed/created (all under `/Users/eric/own/BurgerGo/expo-rn/screens/eats/`):**
- `eatsGoogle.ts` (new) — local helpers: `usePlacesAutocomplete` (per-cycle UUID v4 session token, rotate on select-success/clear; tiny `uuid4()` since `crypto.randomUUID` is unavailable under Hermes and expo-crypto isn't an allowed dep), flat `/api/google/details` fetcher, `forwardGeocode`, `fetchPoiLive` (via `api.google.poi`, failures swallowed), `parseStoredHours` (defensive JSON parse → `[]`), `restaurantThumb` (personal → cached Google → null), `longWeekday` (UTC-stable English long name; lib/days only has "Mon"-style short names).
- `RestaurantCard.tsx` — rewritten to web row layout: hairline-bottom flat row, 72×72 thumb (radius 12, only when photo), name 15/semibold, status pill below name (been = surface/sub, want-to-try = accentTint/accent, 10.5 bold uppercase ls 0.42), meta row with plain-text cuisine + accent/line stars + tabular price, 1-line notes, micro uppercase accent scheduled line, lucide `ChevronRight` 14 faint. Legacy tokens purged.
- `RestaurantDetail.tsx` — rewritten on `SheetPanel` (maxHeight 85%): hairline-box error banners; web content order; **Google block** (gold `Star` fill = `dayColor(2)`, `googleRating.toFixed(1)` semibold tabular, `· n review/reviews`, `· Google` faint; Open now/Closed/Hours disclosure with ▾/▴, live-first stored-fallback hours, openNow never from storage, live fetch on open when online && googlePlaceId with stale-guard, reset on restaurant change); hero with hairline border; gallery + viewer (Previous/Next/Close photo a11y labels); solid-**accent** status toggle (disabled = surface+faint, no opacity); outlined actions; day picker `Day {n} · {Weekday} {date}` with long weekday; two-tap delete (text danger → solid danger "Delete this restaurant?"); close-on-mutate split: `onChanged` (status/schedule/unschedule/delete → parent closes + reloads, matching web) vs `onPhotoChanged` (photo add/delete → reload, sheet stays open). Fixed latent seed bug where `busy` stayed `true` after photo ops.
- `RestaurantForm.tsx` — rewritten on `SheetPanel`: error banner moved to top (web position), sheet title removed (web form has none), Google autocomplete suggestion list (hairline box, description rows, pick fills name-if-empty + address + stores `{lat,lng,googlePlaceId}`, clears list, rotates session; typing after pick invalidates), exact web save-time coordinate resolution (picked → keep-existing → clear-on-empty → forward-geocode changed text + Details warm-call). **Edit** PATCHes full payload incl. address/lat/lng/googlePlaceId (fixes seed bug that dropped location changes); **Add** POSTs then PATCHes `{lat,lng,googlePlaceId}` on the created id when the client resolved a place id (triggers server Google refresh).
- `EatsScreen.tsx` — header polish (title 21/bold/−0.42; orange add button radius 10, 13/semibold, disabled = surface+faint, pressed = orangePress), hairline continuous list (padding 16, no gaps), fade-up entrance stagger `min(i,6)×40ms` via `Animated` (`useNativeDriver` off on web), new detail callback wiring.

**Spec items completed:** all 8 gaps (Google fields already existed in `lib/api/types.ts`; `api.google.poi` already served as the `poiDetails` helper), including nice-to-have #8 (entrance animation). Empty-state-only-when-zero-total, exact copy strings, offline gating preserved.

**Not done / deviations:**
- "links to Google Maps via placeUrl" from the task summary was **intentionally not implemented** — the parity spec explicitly states the web eats detail sheet has no external links ("do not invent any"); spec wins over the summary line.
- `lib/api` `google.details` wrapper types the response as `{details: GooglePlaceDetails}` but the backend returns a flat object; I could not edit `lib/api`, so the form fetches the endpoint directly from `eatsGoogle.ts` (file-ownership-compliant workaround). Worth fixing in `lib/api` later.
- No `geocode` helper exists in `lib/api`; implemented locally for the same reason.

**Contract assumptions:** POST `/api/trips/{id}/restaurants` strips coords + geocodes server-side, and PATCHing a new `googlePlaceId` triggers the server's Google rating/hours/photo refresh (per spec API table); `api.google.autocomplete` always resolves to `{predictions: []}`-compatible JSON (route never 5xxs); review-count pluralization rendered as `· {n} review`/`· {n} reviews` matching the web ICU message.

**Gate:** `npx tsc --noEmit` in `/Users/eric/own/BurgerGo/expo-rn` → exit 0, zero errors.